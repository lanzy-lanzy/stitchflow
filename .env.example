# Semaphore SMS Configuration
# Get your API key from https://semaphore.co
SEMAPHORE_API_KEY=your-actual-semaphore-api-key-here

# Django Secret Key (already in settings.py, but you can override if needed)
# DJANGO_SECRET_KEY=your-secret-key-here
